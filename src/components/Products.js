import React, { useContext } from 'react';

const Products = () => {
	const { products, addItem } = useContext();
		return (
				<div className="products-container">
					
				</div>
		);
};

export default Products;
